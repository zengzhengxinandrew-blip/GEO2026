# GeoLook 引擎与发布凭证（复制为 .env；也可在看板「设置」里填写，会自动写入 .env）
# Engine & publishing credentials. Copy to .env, or fill them in the dashboard Settings page.
# 一个都不填也能跑：自动采样会跳过，无 API 引擎走人工采样表。

# ---- Hosted / server deployment ----
# Public bind requires GEOLOOK_TOKEN. Put HTTPS (Caddy/nginx/cloud proxy) in front.
# GEOLOOK_HOST=0.0.0.0
# GEOLOOK_PORT=8765
# GEOLOOK_TOKEN=
# GEOLOOK_COOKIE_SECURE=1
# GEOLOOK_ENV_FILE=/data/.env
# GEOLOOK_WORK_DIR=/data/work
# GEOLOOK_JOBS_DIR=/data/jobs

# ---- 国内引擎 CN engines ----
ZHIPUAI_API_KEY=
ARK_API_KEY=
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=
MINIMAX_API_KEY=

# ---- 海外引擎 Global engines ----
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
PERPLEXITY_API_KEY=

# 可选：覆盖各引擎默认采样模型 Optional model overrides
# GLM_MODEL=  ARK_MODEL=  DEEPSEEK_MODEL=  MOONSHOT_MODEL=  MINIMAX_MODEL=
# GEMINI_MODEL=  OPENAI_MODEL=  ANTHROPIC_MODEL=  GROK_MODEL=  PERPLEXITY_MODEL=

# ---- 发布渠道（可选）Publishing channels, optional ----
GITHUB_TOKEN=
WP_USER=
WP_APP_PASSWORD=
WECHAT_APPID=
WECHAT_APPSECRET=
PUBLISH_WEBHOOK_URL=
